// const inventory = require('./cars');


function lastCar(inv)
{
    return inv[inv.length-1];
}
    
    // console.log("Last car is a "+car.car_make+" "+car.car_model);
    


module.exports = lastCar;